export TEST_RESULTS=false
npm test && export TEST_RESULTS=true
#if [ "$TEST_RESULTS" = "true" ]; then
#    export TEST_RESULTS=false;
#    cd app && npm test && export TEST_RESULTS=true;
#    cd ..;
#fi
